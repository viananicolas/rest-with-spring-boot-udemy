CREATE TABLE permission (
                            [id] bigint NOT NULL IDENTITY,
                            [description] varchar(255) DEFAULT NULL,
                            PRIMARY KEY ([id])
) ;